[project]
name = "agent-cli-stats"
version = "0.1.0"
description = "CLI tools to display usage statistics for Claude Code and Codex CLI"
requires-python = ">=3.11"
dependencies = ["httpx"]

[dependency-groups]
dev = [
    "ruff>=0.14.14",
]
